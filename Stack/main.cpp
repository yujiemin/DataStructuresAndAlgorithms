#include "VectorStack.h"
#include "ListStack.h"
#include "BigInteger.h"

int main()
{

	// VStack<int> vStack;
	//
	// vStack.Push(1);
	// vStack.Push(2);
	// vStack.Push(3);
	// vStack.Push(4);
	//
	// vStack.Pop();
	// vStack.Display();
	//
	// LStack<int> lStack;
	// lStack.Push(1);
	// lStack.Push(2);
	// lStack.Push(3);
	// lStack.Push(4);
	// lStack.Clear();
	// lStack.Display();

	BigInteger bigInt1("1234");
	BigInteger bigInt2("1239");

	bigInt1 + bigInt2;


	return 0;
}